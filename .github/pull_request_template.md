## Summary

<!-- What changed and why (1–3 sentences) -->

## Checklist

- [ ] Ran `pnpm typecheck && pnpm test && pnpm build`
- [ ] Ran `pnpm docs:check` (if docs or links changed)
- [ ] Updated [`docs/AI-README.md`](../docs/AI-README.md) or the relevant topic guide if behavior or contracts changed
- [ ] Ran `pnpm optimize:blog` (or `optimize:hero`) if new image masters were added
- [ ] Multi-step initiative: workplan in [`docs/workplans/`](../docs/workplans/README.md) linked above (or N/A)
- [ ] New `*Contract.ts` or `package.json` script → row added to AI-README tables (see [Re-sync](../docs/AI-README.md#re-sync-and-drift-prevention-team-norm))

## Test plan

<!-- How you verified the change (routes clicked, commands run, etc.) -->
